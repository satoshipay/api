machine:
  ruby:
    version: "2.4.1"

test:
  override:
    - "true"

deployment:
  production:
    branch: master
    commands:
      - bundle exec middleman build --clean
      - /bin/bash publish.sh
